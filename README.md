# SVG Drawing Pad 🎨

An interactive SVG-based drawing application built using **HTML**, **SVG**, and **JavaScript**.  
This project allows users to draw freehand lines inside an SVG canvas using mouse interactions, similar to a simple digital whiteboard.

---

## 📌 Features

- Click inside the SVG area to start drawing
- Draw continuous lines by moving the mouse
- Release the mouse to stop drawing
- Clear/Delete button to erase all drawings
- Smooth and responsive SVG polyline drawing
- Simple and clean user interface

---

## 🧠 Concepts Used

- SVG inside HTML
- SVG `<polyline>` element
- Mouse events:
  - `mousedown`
  - `mousemove`
  - `mouseup`
  - `mouseleave`
- Capturing mouse coordinates
- Dynamic DOM manipulation using JavaScript

---

## 🛠️ Technologies Used

- HTML5
- SVG
- JavaScript (Vanilla JS)
- GitHub Pages (for hosting)

---

## 📂 Project Structure

svg-drawing-pad/
│
├── index.html
├── script.js
└── README.md
